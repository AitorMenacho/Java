//Ahora vamos a hacer un juego en el que el ordenador eligirá un número de entre un rango que le demos y luego lo tendremos que adivinar.

Algoritmo juego

	definir numAleatorio Como Entero;

	numAleatorio<-Aleatorio(0,10); //Aquí le estamos diciendo a la variable que genere un número de entre 1 y 10.

	Escribir "Ingresa un número de 0 a 10";

	Definir numUsuario Como Entero; // Aquí definimos el número que ha escrito el usuario
 
	leer numUsuario; // Aquí leemos y guardamos dicho número.

	// Para saber si lo hemos adivinado tendremos que comparar el número aleatorio con el ingresado.

	Si numAleatorio = numUsuario Entonces
		Escribir "¡Bien la adivinaste!, el número es:", numAleatorio;
	SiNo
		Escribir "Game Over, el número es: ", numAleatorio;
	Fin Si

	//La manera de arriba es una simple en la que el usuario solo tiene un intento, pero para que el usuario tenga más de un intento tendremos que usar la función Mientras.

	Intentos<-3;

	//Con esta función le que le decimos es que mientras que la variabel "Intentos" sea mayor que 0 tiene que seguir haciendose la función.

	Mientras Intentos>0 Hacer

		Escribir "Ingresa un número de 0 a 10";
		
		leer numUsuario;

		Si numAleatorio = numUsuario Entonces
			Escribir "¡Bien la adivinaste!, el número es:", numAleatorio;
			//En caso de que se acierte habría que igualar Intentos a 0 para que así no se vuelva a ejecutar.
			Intentos<-0;
		SiNo
			//Para conseguir que la varibale "Intentos" llegue a 0 tendremos que ir restando, ya que si no esta función sería infinita.
			Intentos<-Intentos-1;
			Escribir "¡Fallaste!, te quedan: ", Intentos," intentos";
		Fin Si

FinAlgoritmo