Algoritmo Preguntas //Inicio del programa

	Definir edad como Entero; //Definimos la variable edad como un número entero.
	
	Escribir "¿Que edad tienes?" //Al introducir una cadena de texto de tipo string debemos ponerlo entre comillas.

	//En este caso no queremos que tenga un valor definido por nosotros, si no, un valor que defina el usuario.

	leer edad; //Con esto le pedimos al usuario que meta un número para que el programa lo lea y lo meta en la variable.

	Escribir edad; //Este es el nombre de la variable por lo que no tenemos que meterlo entre comillas. Con esto comprobamos que es lo que tiene dentro la variable edad.

	Escribir edad," años"; //Con esto realizamos lo mismo que con lo anterior pero concatenando la palabra años, para ello usaremos una "," en este caso pero en otros se usará un "+".

FinAlgoritmo //Fin del programa

//Tenemos que tener cuidado al nombrar las variables ya que no aceptan carácteres especiales como ñ, tildes o espacios.
//También tenemos que tener siempre en cuenta las mayúsculas y minúsculas, ya que estos programas son sensibles a esto, por lo que no es lo mismo edad y EDAD.